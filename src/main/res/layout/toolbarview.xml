<?xml version="1.0" encoding="utf-8"?>
<com.arcdatum.android.core.ui.other.ToolbarView xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	android:layout_width="match_parent"
	android:layout_height="wrap_content"
	android:orientation="vertical" >

	<View
		android:id="@+id/Background"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:layout_alignBottom="@+id/ToolbarContainer"
		android:layout_alignTop="@+id/ToolbarContainer"
		android:background="@android:color/white" />

	<RelativeLayout
		android:id="@+id/ToolbarContainer"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:orientation="vertical" >

		<android.support.v7.widget.Toolbar
			android:id="@+id/Toolbar"
			android:layout_width="match_parent"
			android:layout_height="?attr/actionBarSize"
			android:layout_alignParentTop="true" >

			<FrameLayout
				android:id="@+id/ToolbarViewMain"
				android:layout_width="match_parent"
				android:layout_height="wrap_content" >
			</FrameLayout>
		</android.support.v7.widget.Toolbar>

		<FrameLayout
			android:id="@+id/ToolbarViewComplementary"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:layout_below="@+id/Toolbar" >
		</FrameLayout>
	</RelativeLayout>

	<View
		android:id="@+id/View_Shadow"
		android:layout_width="match_parent"
		android:layout_height="5dp"
		android:layout_below="@+id/ToolbarContainer"
		android:background="@drawable/shape_gradient_toolbarshadow" />

</com.arcdatum.android.core.ui.other.ToolbarView>